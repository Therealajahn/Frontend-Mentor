<script setup>
	const index = 0;
//let main = document.querySelector('main');

//function makeCard(cardJSON,index){
//	let logo = card.querySelector('img');
//		logo.setAttribute('src',cardJSON.logo);
//	let name = card.querySelector('h3');
//		name.innerHTML = cardJSON.name;
//	let description = card.querySelector('p');
//		description.innerHTML = cardJSON.description;
//	let remove = card.querySelector('button');
//		remove.addEventListener('click',() => {
//			card.style.display = 'none';
//		});
//	let checkbox = card.querySelector('input');
//		checkbox.setAttribute('id',`enable-toggle${index}`);
//		if(cardJSON.isActive){
//			checkbox.setAttribute('checked','true');
//		};
//	let label = card.querySelector('label');
//		label.setAttribute('for',`enable-toggle${index}`);
//
//	main.appendChild(card);
//};

</script>

<template>
	<!--
	<article class="card">
		<img :src="cardJSON.logo">
		<h3 class="name">{{ cardJSON.name }}</h3>
		<p class="description">{{ cardJSON.decription }}</p>
		<button class="remove-button">Remove</button>
		<input type="checkbox" class="enable-toggle">
		<label class="enable-toggle" :for="`enable-toggle${index}`"></label>;
	</article>
	-->
</template>
